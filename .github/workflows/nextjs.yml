name: Deploy Next.js site to Pages

on:
  push:
    branches:
      - '**'
    paths:
      - 'frontend/**'  # Only trigger on changes inside frontend

  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json  # Correct lock file location

      - name: Debug - List Files in frontend
        run: ls -la frontend  # Check if package.json and package-lock.json exist

      - name: Install Dependencies
        run: npm install
        working-directory: frontend  # Run inside the frontend directory

      - name: Build with Next.js
        run: npm run build
        working-directory: frontend  # Ensure build runs in frontend

      - name: Upload Build Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: frontend/out  # Ensure this matches Next.js output directory
  
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
